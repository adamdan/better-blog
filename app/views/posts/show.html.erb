<p id="notice"><%= notice %></p>

<p>
<div class="post-full">

<h2>  <%= @post.title %></h2>

<p> <%= time_ago_in_words(@post.created_at) %> ago</p>

  <%= a = @post.content.html_safe %>
</div>

<p>
  <%= @post.name %>
</p>
<p>
  <b>Tags:</b>
  <% #join_tags(@post) %>
</p>

<h6>Comments</h6>
<div id="comment_list">
<ul>
<%= render @post.comments %>
</ul>

<h4>Add a comment:</h4>
<%= render "comments/form" %>


<%= link_to 'Edit Post', edit_post_path(@post) %> |
<%= link_to 'Back to Posts', posts_path %> |
    </div>